# TrailVerseWidgets

Widget extension (Home Screen, Lock Screen, Live Activity) ships in **Phase 2** after Today mode works.

Generate with XcodeGen after adding `TrailVerseWidgets` target back to `project.yml`.
