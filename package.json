{
  "name": "trailverse-server",
  "version": "1.0.0",
  "description": "TrailVerse - Your Universe of National Parks Exploration",
  "private": true,
  "engines": { "node": "20.x" },
    "scripts": {
      "start": "cd client && npm start",
      "build": "cd client && CI='' npm run build",
      "build:ci": "cd client && CI='' npm run build",
      "test": "cd client && npm test",
      "test:ui": "cd client && npm run test:vitest:ui",
      "e2e": "cd client && npm run test:e2e",
      "e2e:headed": "cd client && npm run test:e2e:headed",
      "format": "cd client && npm run format",
      "lint": "cd client && npm run lint"
    },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "glob": "^11.0.3"
  }
}
